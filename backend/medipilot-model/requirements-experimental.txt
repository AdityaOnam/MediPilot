# Experimental dependencies — NOT installed in the core deployment.
# These are used for the GBDT bake-off (Track C, Group 1) and sequence
# model experiments (Track C, Group 2). They must NOT be added to
# requirements.txt which is what the API deployment installs.
#
# Install with:
#   pip install -r requirements-experimental.txt
#
lightgbm>=4.3
xgboost>=2.0
